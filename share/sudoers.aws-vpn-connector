# awsvpnctl — passwordless sudo for the restricted runner only.
#
# Installed by install.sh to /etc/sudoers.d/aws-vpn-connector.
# The {{USER}} placeholder is substituted at install time.

Defaults!{{SUDO_RUNNER}} !requiretty
{{USER}} ALL=(root) NOPASSWD: {{SUDO_RUNNER}}
